name: Improve automatic round news quality

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/news-quality-v5.yml'
      - 'scripts/patch_news_quality_v5.py'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Apply Arabic-first news patch
        run: python scripts/patch_news_quality_v5.py

      - name: Validate worker JavaScript
        run: node --check cloudflare-worker-v2.js

      - name: Commit worker update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add cloudflare-worker-v2.js
          if git diff --cached --quiet; then
            echo "No changes"
          else
            git commit -m "Prioritize Arabic and trusted round news"
            git push origin main
          fi
